<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MyShop.Views.VagasBusca" Title="Busca por Vagas">
	<StackLayout Margin="20">
		<SearchBar x:Name="searchBar" Placeholder="Endereço para Busca" SearchCommand="{Binding SearchCommand}" SearchCommandParameter="{Binding Source={x:Reference searchBar}, Path=Text}" />
		<ListView ItemsSource="{Binding vagas}" HasUnevenRows="true" ItemTapped="OnListViewItemTapped" ItemSelected="OnListViewItemSelected">
			<ListView.ItemTemplate>
				<DataTemplate>
					<ViewCell>
						<Grid Padding="10">
							<Grid.RowDefinitions>
								<RowDefinition Height="Auto" />
								<RowDefinition Height="*" />
							</Grid.RowDefinitions>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="Auto" />
								<ColumnDefinition Width="*" />
							</Grid.ColumnDefinitions>
							<Image Grid.RowSpan="2" Source="{Binding Foto}" Aspect="AspectFill" HeightRequest="60" WidthRequest="60" />
							<Label Grid.Column="1" Text="{Binding Tipo}" Style="{DynamicResource TitleStyle}" />
							<Label Grid.Row="1" Grid.Column="1" Text="{Binding Endereco}" VerticalOptions="End" Style="{DynamicResource SubtitleTextStyle}" />
						</Grid>
					</ViewCell>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>
	</StackLayout>
	
</ContentPage>